{
  "name": "menu-fit-fabricaprojetos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix menu-fit-brasil-backend\" \"npm run dev --prefix menu-fit-brasil-frontend\"",
    "build": "npm run build --prefix menu-fit-brasil-frontend",
    "backend": "npm run dev --prefix menu-fit-brasil-backend",
    "frontend": "npm run dev --prefix menu-fit-brasil-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
